import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    width: '100%',
    paddingVertical: 10
  },

  listContainer: {
    flexGrow: 1,
    width: '100%',
    paddingVertical: 5
  }
});

export default styles;