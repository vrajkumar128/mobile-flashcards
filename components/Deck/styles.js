import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '85%',
    alignItems: 'center',
    backgroundColor: '#fff',
    marginLeft: 20,
    marginRight: 20,
    marginTop: 20,
    borderRadius: 2
  },

  contents: {
    alignItems: 'center',
    marginTop: 10,
    marginBottom: 10
  }
});

export default styles;